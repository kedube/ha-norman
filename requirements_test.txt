pytest>=8.2.0
pytest-homeassistant-custom-component>=0.13.364
# Used by tests/test_repo_consistency.py to parse services.yaml. Available transitively via
# Home Assistant, but declared explicitly so the suite does not depend on a transitive pin.
PyYAML>=6.0
# Used by tests/test_repo_consistency.py to check the brand images. Also a Home Assistant
# dependency, declared for the same reason.
Pillow>=10.0
ruff>=0.8.0
# Type checking. The integration is annotated throughout and currently checks clean, which is
# the quality scale's Platinum "strict-typing" rule; CI runs this so it stays that way.
mypy>=1.13
getmac==0.9.5
